name:  MO Checkin Regression(Standalone BVT)

on:
  pull_request_target:
    branches: [ main,'[0-9]+.[0-9]+*',Environment-secrets-1 ]
    types:
      - closed
    paths:
      - '**.go'
      - '**.c'
      - '**.h'
      - 'test/distributed/**'

jobs:
  mo-checkin-regression:
    name: MO Checkin Regression(Standalone BVT)
    uses: Rosyrain/CI/.github/workflows/merge-trigger-standalone.yaml@Environment-secrets
    secrets: inherit
